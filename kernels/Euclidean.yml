name: EuclideanKernel
stationary: true

doc: |
    The euclidean kernel is a stationary kernel where the value
    at a given radius :math:`r^2` is given by:

    .. math::

        k(r^2) = \sqrt{r^2}

value: return sqrt(r2);

grad:
    r2: |
        return 0.0;
