name: TransNumEmbedKernel
stationary: false
params: []

doc: |
    The transformer's num_embed kernel.

value: |
    if(x1 != x2) {
        return 0.6;
    }
    else {
        if(x1 == 0) {
            return 0.95;
        }
        else if(x1 == 0.5) {
            return 0.9;
        }
        else if(x1 == 1) {
            return 0.85;
        }
    }

grad:
    x1: |
        return 0.0;

    x2: |
        return 0.0;
