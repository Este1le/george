name: TransNumEmbedKernel
stationary: false
params: []

doc: |
    The transformer's num_embed kernel.

value: |
    if(x1 != x2) {
        return 0.57;
    }
    else {
        return 1;
    }

grad:
    x1: |
        return 0.0;

    x2: |
        return 0.0;
