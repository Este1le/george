name: TransInitialLearningRateKernel
stationary: false
params: []

doc: |
    The transformer's initial_learning_rate kernel.

value: |
    if(x1 != x2) {
        return 0.23;
    }
    else {
        return 1;
    }

grad:
    x1: |
        return 0.0;

    x2: |
        return 0.0;
