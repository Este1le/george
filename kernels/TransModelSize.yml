name: TransModelSizeKernel
stationary: false
params: []

doc: |
    The transformer's transformer_model_size kernel.

value: |
    if(x1 != x2) {
        return 0.54;
    }
    else {
        return 1;
    }

grad:
    x1: |
        return 0.0;

    x2: |
        return 0.0;
