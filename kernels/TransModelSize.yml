name: TransModelSizeKernel
stationary: false
params: []

doc: |
    The transformer's transformer_model_size kernel.

value: |
    if(x1 != x2) {
        return 0.6;
    }
    else {
        if(x1 == 0) {
            return 0.95;
        }
        else {
            return 0.9;
        }
    }

grad:
    x1: |
        return 0.0;

    x2: |
        return 0.0;
