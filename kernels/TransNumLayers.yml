name: TransNumLayersKernel
stationary: false
params: []

doc: |
    The transformer's num_layers kernel.

value: |
    if(x1 != x2) {
        return 0.78;
    }
    else {
        return 1;
    }

grad:
    x1: |
        return 0.0;

    x2: |
        return 0.0;
