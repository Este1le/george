name: TransBpeSymbolsKernel
stationary: false
params: []

doc: |
    The transformer's bpe_symbols kernel.

value: |
    if(x1 != x2) {
        return 0.75;
    }
    else {
        return 0.9;
    }

grad:
    x1: |
        return 0.0;

    x2: |
        return 0.0;
